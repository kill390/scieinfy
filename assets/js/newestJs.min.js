const progress=document.querySelector("progress"),moreLink=document.getElementById("more");let options={};const limit=10;let lastLimit=parseInt(getCookie("lastLimit"));const query=db.collection("recents").orderBy("date","desc").limit(10);let lastDoc;ios()&&enablePersistence(),moreLink.addEventListener("click",(async()=>{let e=await query.startAfter(lastDoc).get();e.empty&&(moreLink.style.display="none"),e.forEach((e=>{queryThings(e)}))}));